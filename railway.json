{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace=@adventure/shared && npm run build --workspace=@adventure/api"
  },
  "deploy": {
    "startCommand": "cd apps/api && npx prisma migrate deploy && cd ../.. && node apps/api/dist/main.js",
    "healthcheckPath": "/api/v1/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
